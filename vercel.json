{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && (node prisma/seed.cjs || true) && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs"
}
